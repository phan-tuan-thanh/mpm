# Plan — Task 3.2: Tách header khỏi nút hành động (members, labels, priorities)

Spec: `project-settings-tab-ui-consistency` (bugfix)

## Approach
Trong flex header row của mỗi tab, xóa CHỈ `<div>` chứa `<h2>` tiêu đề + `<p>` mô tả cấp trang.
Giữ nguyên cụm nút hành động (signatures verbatim mà preservation spec kiểm tra).
Nâng cụm nút hành động lên làm con trực tiếp của root, bỏ `justify-between` (dùng `justify-end` để giữ căn phải).

## Files sửa
- `members-tab/members-tab.component.ts` (inline template)
- `labels-tab/labels-tab.component.ts` (inline template)
- `priorities-tab/priorities-tab.component.html`

## Giữ nguyên verbatim (preservation spec)
- members: `(ngModelChange)="onSearchChange($event)"`, `placeholder="Tìm tên hoặc email..."`, `(click)="showAddDialog()"`, `label="Thêm thành viên"`, guard `!isReadOnly()`
- labels: `(click)="confirmBulkDeleteProj()"`, `[label]="'Xóa ' + projSelected().size"`, `(click)="clearProjSelection()"`, `pTooltip="Bỏ chọn tất cả"`, guard `projSelected().size > 0`
- priorities: `(click)="showAddForm.set(true)"`, `label="Thêm mức"`, guard `!isReadOnly()`

## Ràng buộc
- KHÔNG đổi root spacing token (Task 3.3): members `space-y-4`, labels/priorities `space-y-6`
- KHÔNG đổi TypeScript logic
- KHÔNG sửa spec files

## Acceptance
- NOT hasDuplicatePageHeader (header cấp trang bị bỏ)
- Nút hành động vẫn render + hoạt động
- Diagnostics sạch; preservation spec vẫn pass
- _Requirements: 2.1, 3.1_
